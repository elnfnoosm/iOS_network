name: Rule_Generate

on:
  schedule:
    - cron: '0 20 * * *'
  pull_request:
    branches: [ "main" ]
  repository_dispatch:
    types: [bot_trigger]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 🔥 升级到官方最新 v50，完全支持 Node24，无警告
      - uses: actions/checkout@v50

      - name: Print current directory
        run: |
          cd ./rule/
          pwd

      - name: "拉取"
        run: python rule/Script/Get_File.py

      - name: "合并"
        run: python rule/Script/Merge_File.py

      - name: Commit files
        run: |
          git config --local user.email "wwfnle@gmail.com"
          git config --local user.name "elnfnoosm"
          git add ./*
          git diff --quiet && git diff --staged --quiet || git commit -m "Update Rules"

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GIT_TOKEN }}
          branch: main
          force: false
